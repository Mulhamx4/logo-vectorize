# خطة التحسين والتطوير — logo-vectorize

آخر تحديث: 2026-09-16

هذي خطة عملية مبنية على وضع المشروع الفعلي (راجع [PROJECT_MAP.md](PROJECT_MAP.md) للحالة التفصيلية)، مرتبة حسب الأولوية. كل بند فيه: المشكلة/الفرصة، ليش يستاهل، وحجم الجهد التقريبي (S/M/L).

---

## P0 — قبل ما تعتمد على الأداة بثقة كاملة

### P0-1: اختبار آلي لحجم المعاينة في شاشة المراجعة
**المشكلة اللي اكتشفناها اليوم** (الصورة الأصلية تطلع مكبّرة/مقصوصة) ما كان يمسكها أي اختبار، لأن `tests/e2e.py` يتحقق من نسبة التطابق (بيانات البكسل) بس، وما يتحقق من *حجم وأبعاد* العنصر المعروض فعليًا في `.lv-orig`. يعني أي رجعة لنفس الخلل (regression) بعد أي تعديل CSS ثاني ما راح ينكشف تلقائيًا.
**الحل**: أضف فحص في `tests/e2e.py` بعد `trace()` يقيس `getBoundingClientRect()` لعنصر الـ canvas داخل `.lv-orig` ويتأكد إنه ما يتجاوز حدود `.lv-layer` (بنفس منطق التحقق اللي سويته يدويًا الآن)، وكرره مع صورة مستطيلة جدًا (مثلاً 1:4) وصورة كبيرة (فوق 4000px) عشان تغطي الحالات الحرجة.
**الجهد**: S (نصف يوم)

### P0-2: اختبار حقيقي على iOS Safari و Firefox
موثّق كنقطة ضعف معروفة (GOAL-12 في PROJECT_MAP.md): كل الاختبارات — الآلية واليدوية — صارت على Chromium بس. الأداة تعتمد على Module Workers وOffscreenCanvas وaspect-ratio؛ هذي مدعومة بس فيها فروقات دقيقة بين المحركات (وبالضبط هذا النوع من الفروقات سبب خلل اليوم).
**الحل**: تشغيل يدوي على جهاز iPhone حقيقي + Firefox حقيقي (مو Playwright firefox channel بس، لأنه يختلف شوي عن Firefox الحقيقي)، أو تفعيل BrowserStack/Sauce Labs في CI لاحقًا.
**الجهد**: S (لو عندك جهاز)، M (لو تبي تدمجها في CI)

### P0-3: توسعة Playwright لتغطية Firefox وWebKit في CI
حاليًا `pip install playwright && playwright install chromium` بس. بإضافة `firefox` و`webkit` كمصفوفة (matrix) في `pages.yml`، تنكشف فروقات المحركات في كل push بدل ما تنكتشف يدويًا بعد النشر.
**الجهد**: S — تغيير في `.github/workflows/pages.yml` لعمل matrix على `browser: [chromium, firefox, webkit]`، وتعديل `tests/e2e.py` ليقبل اسم المتصفح كمعامل.

---

## P1 — تحسينات تستاهل في المدى القريب

### P1-1: معالجة أكثر من شعار دفعة وحدة (Batch)
شركة عندها أكثر من شعار (شعار رئيسي + شعارات فرعية لمنتجات) تحتاج ترفع كل واحد لحاله. إضافة "ارفع عدة ملفات" مع قائمة نتائج، وتحميل ZIP واحد لكل الشعارات، يوفر وقت حقيقي.
**الجهد**: M

### P1-2: استئناف الجلسة بعد تحديث الصفحة
موثّق كسلوك حالي: "Refresh at any point → صفحة رفع جديدة (ما يحفظ شي إلا اللغة والثيم)". لو المستخدم قفل التبويب غلط بعد ما ضبط الألوان، يخسر كل شي. حفظ آخر نتيجة (الصورة + الخيارات) في IndexedDB مؤقتًا (مع تنظيف تلقائي بعد مدة) يحل هذا بدون ما يكسر مبدأ "الشعار ما يطلع من الجهاز".
**الجهد**: M

### P1-3: تدقيق وصولية (accessibility) كامل
الكود فيه أساس جيد (`role="slider"`, `aria-label`, `aria-valuenow`...)، لكن ما فيه تدقيق WCAG رسمي موثّق. تشغيل axe-core ضمن `tests/e2e.py` كخطوة تلقائية يمسك الانحدار (regressions) بدري.
**الجهد**: S–M

### P1-4: تحديث ملاحظة Node 20 في GitHub Actions
التحذير اللي طلع بعد أول نشر ("Node.js 20 is deprecated... forced to run on Node.js 24") مو عاجل — GitHub يرفعها تلقائيًا حاليًا — لكن يستاهل متابعة إصدارات `actions/checkout` و`actions/setup-python` الجديدة بعد شهرين-ثلاثة عشان ما تتوقف فجأة.
**الجهد**: XS (متابعة دورية بس)

### P1-5: نشر حزمة npm مستقلة
حاليًا `site/lib/` تُستخدم بالنسخ المباشر (INTEGRATION.md يوثق هذا). نشرها كحزمة npm (`@mulhamx4/logo-vectorizer` مثلاً) يسهّل التحديث في Brand Kit Builder وصفحة الخطوط بدون نسخ يدوي، ويفتح المجال لمشاريع ثانية تستخدمها.
**الجهد**: M — يحتاج ضبط build خفيف (حتى لو بقيت ES modules بدون bundler)، ونشر على npm تحت ترخيص GPL-2.0-or-later.

---

## P2 — تحسينات منتج للمدى المتوسط

### P2-1: تحرير أدق للفيكتور بعد التتبع
حاليًا التحرير المتاح: الألوان، الخلفية، تعبئة المساحات المحصورة. إضافة تبسيط/تنعيم المسارات (path simplification slider) تفيد لما potrace يطلع نقاط زايدة على شعار فيه تفاصيل دقيقة.
**الجهد**: L

### P2-2: معاينة حية أثناء تعديل الخيارات
حاليًا تغيير خيار (لون، خلفية) يفعّل زر "إعادة التتبع" وتحتاج تضغطه. معاينة تقريبية فورية (قبل التتبع الكامل) تحسن تجربة الضبط.
**الجهد**: L

### P2-3: ربط فعلي مع Brand Kit Builder وصفحة الخطوط
INTEGRATION.md يوثق إنها أمثلة مبنية ومجربة، لكن ما انربطت فعليًا بالمنتجين الحقيقيين بعد. هذا أكبر بند "جاهز للإلحاق" غير منفّذ.
**الجهد**: M–L (يعتمد على وصول لمستودعي المشروعين الثانيين)

### P2-4: مشاركة نتيجة بدون تحميل
رابط مؤقت (blob URL أو query-encoded state صغير) لمشاركة نتيجة المراجعة مع زميل قبل التحميل النهائي — مفيد لموافقات سريعة داخل فريق.
**الجهد**: M

---

## ملخص الأولويات

| # | البند | الأثر | الجهد |
| --- | --- | --- | --- |
| P0-1 | اختبار حجم المعاينة (يمنع رجوع خلل اليوم) | عالي | S |
| P0-2 | اختبار حقيقي iOS Safari / Firefox | عالي | S–M |
| P0-3 | Firefox + WebKit في CI | عالي | S |
| P1-1 | رفع عدة شعارات دفعة وحدة | متوسط–عالي | M |
| P1-2 | حفظ الجلسة مؤقتًا | متوسط | M |
| P1-3 | تدقيق وصولية WCAG | متوسط | S–M |
| P1-5 | نشر كحزمة npm | متوسط | M |
| P2-1 | تبسيط المسارات بعد التتبع | متوسط | L |
| P2-3 | ربط فعلي بـ Brand Kit وصفحة الخطوط | عالي (لو مطلوب قريب) | M–L |

**التوصية**: ابدأ بـ P0-1 لوحده أول شي — نصف يوم يقفل أكبر فجوة اكتشفناها اليوم (خلل بصري ما يمسكه أي اختبار)، وبعدها P0-3 لأنه يفتح الباب لاكتشاف فروقات محركات ثانية بدون ما تحتاج جهاز فعلي كل مرة.
