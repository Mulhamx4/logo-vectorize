# خطة التحسين والتطوير — logo-vectorize

آخر تحديث: 2026-09-17

خطة مبنية على وضع المشروع الفعلي (راجع [PROJECT_MAP.md](PROJECT_MAP.md) للتفاصيل)، مرتبة حسب الأولوية. كل بند فيه المشكلة، وليش يستاهل، وحجم الجهد (S/M/L)، وحالته.

## الحالة

| # | البند | الحالة |
| --- | --- | --- |
| P0-1 | اختبار حجم المعاينة في شاشة المقارنة | ✅ منفّذ — [#1](https://github.com/Mulhamx4/logo-vectorize/pull/1) |
| P0-2 | اختبار على آيفون وأندرويد وFirefox حقيقية | ⏸️ مؤجّل، يحتاج أجهزة — [#2](https://github.com/Mulhamx4/logo-vectorize/issues/2) |
| P0-3 | Firefox وWebKit في CI | ✅ منفّذ — [#1](https://github.com/Mulhamx4/logo-vectorize/pull/1) |
| P1-1 | عدة شعارات دفعة وحدة | ✅ منفّذ — [#1](https://github.com/Mulhamx4/logo-vectorize/pull/1) |
| P1-2 | الاستكمال بعد تحديث الصفحة | ✅ منفّذ — [#1](https://github.com/Mulhamx4/logo-vectorize/pull/1) |
| P1-3 | فحص الوصولية WCAG | ✅ منفّذ — [#1](https://github.com/Mulhamx4/logo-vectorize/pull/1) |
| P1-4 | إصدارات GitHub Actions على Node 24 | ✅ منفّذ — [#1](https://github.com/Mulhamx4/logo-vectorize/pull/1) |
| P1-5 | حزمة npm | 🟡 الحزمة جاهزة ومجرّبة، النشر مؤجّل (يحتاج حسابك) — [#3](https://github.com/Mulhamx4/logo-vectorize/issues/3) |
| P2-1 | التحكم بنعومة المسارات | ✅ منفّذ كإعداد «الزوايا» — [#1](https://github.com/Mulhamx4/logo-vectorize/pull/1) |
| P2-2 | معاينة حية أثناء تعديل الإعدادات | ⏸️ مؤجّل، يحتاج قرار تصميم — [#5](https://github.com/Mulhamx4/logo-vectorize/issues/5) |
| P2-3 | الربط مع Brand Kit Builder وصفحة الخطوط | ⏸️ مؤجّل، تعديل منتجين ثانيين — [#4](https://github.com/Mulhamx4/logo-vectorize/issues/4) |
| P2-4 | مشاركة نتيجة بدون تحميل | ⏸️ مؤجّل، يتعارض مع وعد الخصوصية — [#6](https://github.com/Mulhamx4/logo-vectorize/issues/6) |

---

## P0 — قبل ما تعتمد على الأداة بثقة كاملة

### P0-1: اختبار آلي لحجم المعاينة في شاشة المراجعة ✅
**المشكلة:** خلل تكبير الصورة الأصلية ما كان يمسكه أي اختبار، لأن `tests/e2e.py` كان يتحقق من نسبة التطابق (بيانات البكسل) بس، مو من حجم العنصر المعروض.
**اللي انعمل:** فحص `stage_fits` يقيس الـ canvas الأصلي والـ SVG داخل نفس الصندوق، ويتأكد إنهم داخل الحدود، وبنفس النسبة، ومتطابقين فوق بعض بفرق أقل من 2 بكسل. يشتغل على صور طويلة (1:6) وعريضة (7:1) وكبيرة (4200px) وعلى عرض الجوال، في المحركات الثلاثة.
**الجهد:** S

### P0-2: اختبار حقيقي على iOS Safari وFirefox ⏸️
**ليش مؤجّل:** يحتاج أجهزة حقيقية. المحركات الثلاثة تُختبر الحين في CI، لكن Playwright مو نفس Safari على آيفون (الذاكرة، حد الـ canvas، اللمس، منتقي الملفات).
**التالي:** قائمة الفحص كاملة في [#2](https://github.com/Mulhamx4/logo-vectorize/issues/2).
**الجهد:** S لو عندك جهاز

### P0-3: Firefox وWebKit في CI ✅
**اللي انعمل:** `pages.yml` يشغّل الاختبارات كمصفوفة على `chromium` و`firefox` و`webkit`، والنشر ينتظر الثلاثة. `tests/e2e.py` يستقبل اسم المتصفح.
**اللي انكشف فورًا:** WebKit يرفض حفظ `Blob` في IndexedDB في النوافذ الخاصة، فكان الاستكمال بيفشل في Safari الخاص. انصلح بحفظ الملف كبايتات (موثّق في PROJECT_MAP.md).
**الجهد:** S

---

## P1 — تحسينات المدى القريب

### P1-1: عدة شعارات دفعة وحدة ✅
**اللي انعمل:** حتى 20 شعار بالمرة. كل شعار له إعداداته، وشريط أعلى المراجعة للتنقل، والشعار اللي يفشل يوضّح السبب ويقدر المستخدم يعيده أو يحذفه، و«حمّل كل الشعارات» يطلع ZIP فيه مجلد لكل شعار.
**الجهد:** M

### P1-2: الاستكمال بعد تحديث الصفحة ✅
**اللي انعمل:** `mount({ persist: true })` يحفظ آخر مجموعة (الملفات الأصلية + الإعدادات المطبّقة) في IndexedDB لمدة 24 ساعة، ويعرض «كمّل من حيث وقفت». «شعار جديد» يمسحها. المجموعات فوق 64 م.ب ما تنحفظ. مفعّل في الصفحة المستقلة، ومطفأ في العنصر إلا بخاصية `persist`. الشعار ما يطلع من الجهاز.
**الجهد:** M

### P1-3: فحص الوصولية ✅
**اللي انعمل:** axe-core 4.13 (في `tests/vendor/`) يفحص WCAG 2.2 A/AA على شاشة الرفع، والمراجعة (عربي، إنجليزي، داكن)، وعرض الاستكمال، والمعالجة الجماعية. لقى `aria-pressed` على `<label>` في زر «لون محدد» وانصلح.
**الجهد:** S

### P1-4: GitHub Actions على Node 24 ✅
**اللي انعمل:** `checkout@v7` و`setup-python@v7` و`configure-pages@v6` و`upload-pages-artifact@v5` و`deploy-pages@v5`.
**الجهد:** XS

### P1-5: حزمة npm 🟡
**اللي انعمل:** `package.json` لحزمة `logo-vectorizer` (الاسم متاح)، فيها `site/lib/` فقط. تجربة فعلية: ثبّتها من ملف الحزمة داخل مشروع Vite 8.3 + React 19.3 + TypeScript 6.0.3؛ نجح `tsc` و`vite build` وخادم التطوير، واشتغل تتبع عدة شعارات وتصدير PDF وزر المضيف. التوثيق في INTEGRATION.md.
**ليش النشر مؤجّل:** يحتاج حساب npm خاص بك — [#3](https://github.com/Mulhamx4/logo-vectorize/issues/3).
**الجهد المتبقي:** XS

---

## P2 — تحسينات المنتج للمدى المتوسط

### P2-1: التحكم بنعومة المسارات ✅
**اللي انعمل:** إعداد «الزوايا» بثلاث قيم: حادة، متوازنة (الافتراضي، نفس النتيجة السابقة)، ناعمة. يضبط `alphamax` و`opttolerance` في potrace. على شعار الاختبار: الحادة تطلع 145 مقطع مستقيم، والمتوازنة 50، والناعمة صفر، والتطابق يظل 100%. نفس الجدول في مهارة Python عبر `--corners`.
**الجهد:** S

### P2-2: معاينة حية أثناء تعديل الإعدادات ⏸️
**ليش مؤجّل:** أغلب الإعدادات تغيّر تصنيف البكسلات وتحتاج تتبع كامل، فالمعاينة التقريبية ممكن توري نتيجة غير اللي بتتحمّل. الاقتراحات والقرار المطلوب في [#5](https://github.com/Mulhamx4/logo-vectorize/issues/5).
**الجهد:** M–L

### P2-3: الربط مع Brand Kit Builder وصفحة الخطوط ⏸️
**ليش مؤجّل:** تغيير واجهة منتجين شغّالين في مستودعين ثانيين، ويحتاج قرار مكان الأداة في كل منتج. التفاصيل في [#4](https://github.com/Mulhamx4/logo-vectorize/issues/4).
**الجهد:** M

### P2-4: مشاركة نتيجة بدون تحميل ⏸️
**ليش مؤجّل:** أي رابط مشاركة يا يحتاج خادم يخزن الشعار، يا يحط الشعار داخل الرابط؛ الاثنين يكسرون وعد «الشعار ما يطلع من جهازك». بدائل آمنة وقرار مطلوب في [#6](https://github.com/Mulhamx4/logo-vectorize/issues/6).
**الجهد:** S–M حسب القرار
